import 'package:flutter/material.dart';

class DrawerInfor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: ListView(
        children: [
          Text("Test"),
        ],
      ),
    );
  }
}
